<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NumberConvert" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_light</item>
        <item name="colorPrimaryVariant">@color/primary_light_variants</item>
        <item name="colorOnPrimary">@color/on_primary_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primary_light</item>
        <item name="colorSecondaryVariant">@color/primary_light</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Custom text primary color -->
    <style name="styleColorText">
        <item name="android:textColor">@color/primary_light</item>
    </style>

    <!-- Custom line color -->
    <style name="styleLine">
        <item name="android:background">@color/black</item>
    </style>

    <!-- Custom line color -->
    <style name="styleBackgroundPrimary">
        <item name="android:background">#f5f5f0</item>
    </style>
</resources>